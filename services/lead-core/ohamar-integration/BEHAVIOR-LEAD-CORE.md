# Lead Core — rules (include / link from BEHAVIOR.md on feature branch)

## Enforce (không chỉ “nên”)

1. Khi conversation đã có trên Lead Core: **mọi tin ra** phải `POST .../outbound` → `allowed` trước khi `zaloclaw` send.
2. `409` / sai owner → **im**, escalate owner/human, không gửi đè.
3. Handoff sang Minh Phát chủ động Zalo: cần consent `type=zalo` **grant** còn hiệu lực.
4. Không bịa lead; không lưu SĐT ngoài Core khi đã dùng Core.
5. Vicamed duyệt brand-kits — bot không tự promote claim từ web watch.

## Caller

- Gia Huy → `gia_huy`
- Minh Phát → `minh_phat`
